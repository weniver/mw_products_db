<% provide(:title, ("#{@remission.store.name}: Remisión #{@remission.id}")) %>
<% if @remission.active? %>
  <%= render 'remission_active' %>
<% else %>
  <%= render 'remission_not_active' %>
<% end %>
